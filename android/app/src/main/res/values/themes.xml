<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme required by the activity window; Compose takes over from here -->
    <style name="Theme.DiktierApp" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
